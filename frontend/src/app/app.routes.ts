import { Routes } from '@angular/router';
import { BackendStatusComponent } from './backend-status/backend-status';

export const routes: Routes = [
  {
    path: 'backend-status',
    component: BackendStatusComponent,
  },
];